/** TODO LAB-201: listSessions(subjectId), createSession */
export const useSessions = (subjectId) => {
  return { sessions: [], loading: false, error: null, createSession: async () => {} };
};

export default useSessions;
