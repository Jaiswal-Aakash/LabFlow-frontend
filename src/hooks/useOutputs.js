/** TODO LAB-201: listOutputs, uploadOutputs, updateOutput, deleteOutput */
export const useOutputs = (subjectId, sessionId) => {
  return { outputs: [], loading: false, upload: async () => {}, update: async () => {} };
};

export default useOutputs;
