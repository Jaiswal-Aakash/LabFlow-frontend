/** TODO LAB-201: labApi.listSubjects + createSubject with useAuth token */
export const useSubjects = () => {
  return { subjects: [], loading: false, error: null, createSubject: async () => {} };
};

export default useSubjects;
